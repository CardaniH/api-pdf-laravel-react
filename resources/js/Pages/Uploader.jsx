// resources/js/Pages/Uploader.jsx

import PdfUploader from '@/Components/PdfUploader'; // El alias '@' apunta a 'resources/js'

export default function Uploader() {
    return (
        <main>
            <PdfUploader />
        </main>
    );
}